package practice.coding;

/*
SELECT
	ER.NAME AS NAME,
	CASE
		WHEN ER2.NAME IS NULL THEN 'CEO'
		WHEN (
			SELECT
				count(NAME)
			FROM
				EMPLOYEE_RELATIONSHIPS
			WHERE
				BOSS = ER.NAME > 0
		) THEN 'MANAGER'
		ELSE 'WORKER'
	END AS BOSS
FROM
	EMPLOYEE_RELATIONSHIPS ER
	LEFT JOIN EMPLOYEE_RELATIONSHIPS ER2
	ON ER.BOSS = ER2.NAME
ORDER BY ER.NAME;

* */

public class Test5 {
}
